# Odoo Manager Compose — dependencias de runtime (una única versión).
# Instalar: pipx install odoo-manager-compose  (o pip install ... --break-system-packages)
# (o en venv: pip install -r requirements.txt && pip install --no-deps .). Los paquetes de migración OCA
# (odoo-module-migrator, openupgradelib) se ofrecen instalar a demanda
# al correr `omc migrar` si faltan.
flask>=3.0
gunicorn>=21.2
